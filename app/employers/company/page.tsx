'use client';import {useState} from 'react';export default function CompanyPage(){const[name,sN]=useState('Moja firma s.r.o.');const[website,sW]=useState('https://');function save(){alert('Uložené (DEMO): '+name+' '+website)}return(<main className='container section'><h2 style={{color:'var(--accent)'}}>Profil firmy</h2><div className='card' style={{display:'grid',gap:10}}><input placeholder='Názov firmy' value={name} onChange={e=>sN(e.target.value)}/><input placeholder='Web' value={website} onChange={e=>sW(e.target.value)}/><button className='btn solid' onClick={save}>Uložiť</button></div></main>)}