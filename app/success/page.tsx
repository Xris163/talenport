'use client';import {useEffect,useState} from 'react';export default function Success(){const[m,sM]=useState('Overujem platbu…');useEffect(()=>{const id=new URL(location.href).searchParams.get('session_id');if(!id)return sM('Chýba session_id');fetch('/api/get-session?session_id='+id).then(r=>r.json()).then(j=>sM(j.error?'Chyba: '+j.error:'Platba úspešná. Session: '+j.session.id)).catch(e=>sM('Chyba: '+e.message))},[]);return(<main className='container section'><h2 style={{color:'var(--accent)'}}>Platba</h2><p>{m}</p></main>)}